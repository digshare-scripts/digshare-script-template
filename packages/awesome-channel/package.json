{
  "name": "@digshare/awesome-channel",
  "version": "0.0.0",
  "description": "一个好频道👍",
  "main": "out/index.js",
  "files": [
    "out"
  ],
  "digshare": {
    "runtime": "nodejs14"
  },
  "scripts": {
    "build": "yarn ts-build && dss build -i bld/library/index.js",
    "dev-run": "cross-env DIGSHARE_ENV=development ts-node src/library/@run.ts",
    "prepublishOnly": "yarn build",
    "ts-build": "rimraf ./bld && tsc --build src/library/tsconfig.json"
  },
  "dependencies": {
    "@digshare/script": "^0.1.2",
    "@types/cheerio": "^0.22.30",
    "@types/node-fetch": "^2.5.12",
    "cheerio": "^1.0.0-rc.10",
    "node-fetch": "^2.6.5",
    "tslib": "^2.3.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "ts-node": "^10.4.0"
  }
}
